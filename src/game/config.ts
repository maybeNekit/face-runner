// ВСЕ константы ощущения игры живут здесь и больше нигде.
//
// Правило простое: если число влияет на то, как игра ощущается, — его место
// в этом файле. Разбросанные по коду магические числа невозможно настраивать,
// потому что непонятно, где они и как связаны друг с другом.

// ---------- Дорожки ----------

export const LANE_COUNT = 3
export const LANE_WIDTH = 2.2
/** Координата X центра каждой дорожки. */
export const LANE_X = [-LANE_WIDTH, 0, LANE_WIDTH]
/** Время перестроения. Больше — вязко, меньше — телепортация. */
export const LANE_CHANGE_TIME = 0.2
/** Наклон персонажа в повороте, радианы. */
export const LANE_TILT = 0.38
/** Бамп наклона «упёрся в стену», когда дорожки слева/справа больше нет. */
export const LANE_BLOCKED_TILT = 0.06

// ---------- Скорость и сложность ----------

export const SPEED_START = 8
export const SPEED_MAX = 22
/** Первые секунды — заведомо лёгкие: ребёнок успевает понять правила. */
export const EASY_DURATION = 25
/**
 * До этой секунды препятствий нет вообще — только монетки.
 *
 * 11, а не 6: за 6 секунд ребёнок видит всего две дорожки монет, и с
 * вероятностью 42% ни одна из них не идёт дугой. То есть в двух забегах
 * из пяти он встречал первое препятствие, ни разу не имея повода прыгнуть,
 * и даже не знал, что свайп вверх что-то делает.
 */
export const OBSTACLE_START_TIME = 11
/** Монетки начинаются почти сразу: они и учат управлению. */
export const COIN_START_TIME = 1.5
/** Постоянная времени насыщения разгона: чем больше, тем дольше разгон. */
export const SPEED_RAMP_TAU = 42

// ---------- Прыжок ----------

export const JUMP_VELOCITY = 11
/** Гравитация на подъёме и на падении разная — падение должно быть быстрее,
 *  иначе прыжок ощущается «плавающим». Отношение 1.53 — в правиле 1.3–1.6. */
export const GRAVITY_UP = 30
export const GRAVITY_DOWN = 46

// ---------- Прощение ошибок ----------

/** Прыжок засчитывается ещё некоторое время после схода с земли. */
export const COYOTE_TIME = 0.1
/** Нажатие до приземления не теряется, а срабатывает при касании земли.
 *  0.18, а не 0.13: свайп ребёнка сам по себе длится 150–300 мс. */
export const INPUT_BUFFER_TIME = 0.18
/**
 * Свайп вниз в воздухе роняет персонажа вниз.
 *
 * Без этого прыжок длится 0.66 с, буфер подката — 0.18 с, и свайп вниз,
 * сделанный в воздухе, просто терялся: ребёнок приземлялся стоя и врезался
 * в балку. Это был худший провал прощения во всей игре.
 */
export const AIR_DIVE_VELOCITY = 20
export const AIR_DIVE_BUFFER = 0.38

// ---------- Подкат ----------

export const SLIDE_DURATION = 0.55

// ---------- Персонаж ----------

export const PLAYER_HEIGHT = 1.7
export const PLAYER_RADIUS = 0.34
export const PLAYER_SLIDE_HEIGHT = 0.8
/** Хитбокс меньше модели — всегда в пользу ребёнка. */
export const HITBOX_SCALE = 0.85
/**
 * Полуширина хитбокса как доля ширины дорожки.
 *
 * 0.42 даёт 0.462 м против визуальных 0.52 м по кончикам рук — 89%, как
 * требует правило. При 0.7 хитбокс был 0.77 м, то есть на 48% ШИРЕ модели,
 * и ребёнка било то, что визуально в него не попадало.
 */
export const HITBOX_WIDTH = 0.42

// Squash & stretch
export const STRETCH_JUMP = 1.18
export const SQUASH_LAND = 0.78
export const SQUASH_RECOVER = 9
export const HEAD_BOB = 0.045
/** Радостный подскок на каждой монетке. */
export const COIN_SQUASH = 1.06

// ---------- Камера ----------

export const CAMERA_HEIGHT = 3.4
export const CAMERA_DISTANCE = 6.8
export const CAMERA_LOOK_AHEAD = 7
/** Скорость догона камеры. Жёсткая привязка убивает ощущение скорости. */
export const CAMERA_LAG = 9
/** Насколько камера повторяет смещение игрока по дорожкам.
 *  Меньше единицы даёт параллакс, но слишком мало — и на крайней дорожке
 *  персонаж уезжает к самому краю экрана. */
export const CAMERA_FOLLOW = 0.78
/** Насколько за игроком следует точка, куда камера смотрит. */
export const CAMERA_LOOK_FOLLOW = 0.7
export const FOV_BASE = 62
/** Прибавка к FOV на максимальной скорости — продаёт разгон. */
export const FOV_SPEED_GAIN = 9
/** Схлопывание FOV в момент удара — «дух вышибло». */
export const FOV_CRASH_KICK = -7

// ---------- Тряска камеры ----------

// Тряска — это ПОВОРОТ камеры, а не сдвиг. Сдвиг почти полностью гасился
// доворотом lookAt в ту же точку, и при SHAKE_CRASH 0.5 персонаж смещался
// всего на ~13 пикселей, а дальние объекты — вообще ни на сколько.
export const SHAKE_JUMP = 0.18
export const SHAKE_LAND = 0.22
export const SHAKE_CRASH = 0.5
export const SHAKE_DECAY = 5.5
export const SHAKE_MAX = 0.6
/** Угол при trauma² = 1, радианы. */
export const SHAKE_ANGLE = 0.13
/** ≈7 Гц — частота удара. Белый шум по кадрам читался бы как строб и
 *  зависел бы от частоты экрана. */
export const SHAKE_FREQUENCY = 42

// ---------- Дорога ----------

export const SEGMENT_LENGTH = 20
export const SEGMENT_COUNT = 10
/**
 * Куда ставим появляющиеся объекты.
 *
 * Держим ровно на границе тумана: дальше объект всё равно не видно, зато
 * каждый лишний метр — это задержка между спавном и моментом, когда объект
 * доедет до игрока. При -160 препятствие, порождённое на 6-й секунде,
 * приезжало только на 26-й, и вся кривая сложности разъезжалась с
 * реальными ощущениями.
 */
export const SPAWN_Z = -120
/** За камерой — в пул. */
export const DESPAWN_Z = 14
export const ROAD_WIDTH = LANE_WIDTH * 3 + 1.2

export const FOG_NEAR = 50
/** Чуть дальше SPAWN_Z, чтобы объект появлялся полностью в дымке. */
export const FOG_FAR = 128

// ---------- Спавн препятствий ----------

/**
 * Гэп до следующего препятствия считается во ВРЕМЕНИ на реакцию,
 * а не в метрах: иначе с ростом скорости они начинают прилетать внезапно.
 *
 * Минимум 1.45 с = время в воздухе после прыжка (0.66) + реакция выбора
 * у ребёнка 6–10 лет (0.7) + запас. При 1.05 после прыжка оставалось
 * 0.39 с на «увидеть, понять, сделать» — меньше, чем реакция взрослого.
 */
export const REACTION_TIME_START = 2.4
export const REACTION_TIME_MIN = 1.45
export const REACTION_RAMP_TAU = 70

/** Пока идёт разминка, группа перекрывает ровно одну дорожку из трёх. */
export const SINGLE_LANE_UNTIL = 26
export const DOUBLE_LANE_CHANCE = 0.42
/**
 * Доля групп-передышек. Раньше пустые группы получались случайно и
 * непредсказуемо (14.5%), из-за чего ребёнок не мог выучить темп.
 */
export const PATTERN_EMPTY_CHANCE = 0.18

// ---------- Монетки ----------

export const COIN_VALUE = 10
/** Сколько секунд держится комбо без новой монетки. */
export const COMBO_WINDOW = 2.2
export const COMBO_MAX = 8
export const COIN_RUN_LENGTH = 5
export const COIN_SPACING = 2.6
/**
 * Сколько ступеней тона проходит комбо, прежде чем упереться в потолок.
 *
 * Без потолка на комбо 8 звучало 1976 Гц с гармоникой почти 3 кГц — на
 * динамике телефона это визг. Счёт множится дальше, а звук остаётся добрым.
 */
export const COMBO_PITCH_STEPS = 5

// ---------- Размеры пулов ----------

export const POOL_OBSTACLES = 24
export const POOL_COINS = 72
export const POOL_PARTICLES = 180
export const POOL_STREAKS = 28
export const POOL_SCENERY = 44

// ---------- Бег ----------

/**
 * Частота шагов: база + вклад скорости.
 *
 * Было `speed * 1.15`, и на 22 м/с получалось 8 шагов в секунду — ноги
 * превращались в вибрацию. Теперь на максимуме 4.8 шага/с: быстро, но глаз
 * ещё читает шаг.
 */
export const RUN_CYCLE_BASE = 6
export const RUN_CYCLE_SPEED_GAIN = 0.42
/** Клуб пыли раз в столько метров — привязка к дистанции, а не к кадрам. */
export const DUST_INTERVAL_METERS = 1.2

// ---------- Смерть ----------

export const DEATH_ANIM_DURATION = 1.1
export const DEATH_TUMBLE_SPEED = 11
/** Затухание вращения: без него персонаж жужжал волчком лёжа на асфальте
 *  ещё 1.2 секунды после приземления. */
export const DEATH_TUMBLE_DAMP = 1.8
export const DEATH_LAUNCH_VELOCITY = 9.5
/** Комичный отскок от земли. */
export const DEATH_BOUNCE = 0.35
/** Кувырок катится на камеру — падение читается крупнее. */
export const DEATH_BACK_VELOCITY = 3
/** Как быстро останавливается мир после удара. */
export const DEATH_WORLD_STOP = 3.4

// ---------- Гримасы на бегу ----------

/**
 * Раз в столько секунд персонаж корчит рожу и издаёт нелепый звук.
 *
 * 5–11, а не 3–6.5: при прошлых числах рожа выходила в среднем каждые
 * 4.7 секунды, то есть 12–13 раз в минуту. На такой частоте шутка перестаёт
 * быть шуткой и начинает читаться как «игра постоянно делает меня уродом» —
 * особенно если в игре лицо другого ребёнка. Реже значит смешнее.
 */
export const GRIMACE_GAP_MIN = 5
export const GRIMACE_GAP_MAX = 11
export const GRIMACE_DURATION = 0.45
/** Насколько сильно вершины лица выгибаются наружу. */
export const GRIMACE_STRENGTH = 0.22

// ---------- Бонусы-модификаторы ----------

export const POWERUP_DURATION = 8
/**
 * «Зеркало» живёт заметно меньше остальных бонусов.
 *
 * За 8 секунд успевает прийти 3–5 групп препятствий, а разворот «лево↔право»
 * съедает у шестилетки лишние 0.3–0.5 с из 1.45 с бюджета реакции. Бонус,
 * который гарантированно убивает, — уже не подарок, а наказание.
 */
export const MIRROR_DURATION = 3.5
/** Через сколько метров может появиться следующий бонус. */
export const POWERUP_GAP_MIN = 170
export const POWERUP_GAP_MAX = 300

export const GIANT_HEAD_SCALE = 2.5
export const TINY_BODY_SCALE = 0.55
/** «Желе»: амплитуда и частота колыхания частей тела. */
export const JELLY_AMPLITUDE = 0.17
export const JELLY_FREQUENCY = 8.5
/** Реактивные ботинки: слабее гравитация и выше прыжок. */
export const JET_GRAVITY_SCALE = 0.55
export const JET_JUMP_BOOST = 1.22
/** Как быстро персонаж принимает новую форму. */
export const MODIFIER_MORPH_SPEED = 7

// ---------- Комментатор ----------

export const ANNOUNCE_EVERY_METERS = 100
export const ANNOUNCE_DURATION_MS = 1500

// ---------- Пасхалка ----------

export const EASTER_EGG_METERS = 500

// ---------- Биомы ----------

/** Через сколько метров меняется биом. */
export const BIOME_LENGTH = 300
/** Плавный переход палитры, секунд. Резкая смена била бы по глазам. */
export const BIOME_TRANSITION = 2
